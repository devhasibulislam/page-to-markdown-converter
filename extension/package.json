{
  "name": "markdrop-extension",
  "version": "0.1.0",
  "private": true,
  "description": "MarkDrop browser extension — capture rendered HTML from any page and convert it to Markdown.",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "zip": "cd dist && zip -r ../../dist/extension.zip ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
